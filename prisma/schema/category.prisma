model Category {
    id          String  @id @default(uuid())
    name        String  @unique
    description String?

    tutorLinks TutorCategory[]
    bookings   Booking[]

    createdAt DateTime @default(now())
}
